# niri-tui

a scrolling tiling window manager (similar to niri).

when using the `@opencode-ai/sdk` refer to `./opencode-sdk-reference.md`

Use the `SPEC.md` file to track work. update it when needing a new task or completing a task.
